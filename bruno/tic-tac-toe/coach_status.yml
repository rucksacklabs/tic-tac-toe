info:
  name: coach_status
  type: http
  seq: 7

http:
  method: GET
  url: http://{{host}}/coach/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
