info:
  name: create_game
  type: http
  seq: 1

http:
  method: POST
  url: http://{{host}}/games
  auth: inherit

runtime:
  variables:
    - name: host
      value: localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
