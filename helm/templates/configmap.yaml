apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tic-tac-toe.fullname" . }}
  labels:
    {{- include "tic-tac-toe.labels" . | nindent 4 }}
data:
  DATABASE_URL: {{ .Values.config.databaseUrl | quote }}
  AI_COACH_MODEL: {{ .Values.config.aiCoachModel | quote }}
